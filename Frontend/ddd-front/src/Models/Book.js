class Book {
  constructor( id, title, author, publisher, year) {
    this.id = id;
    this.title = title;
    this.author = author;
    this.publisher = publisher;
    this.year = year;
  }
}

export default Book;
